import { motion } from "framer-motion";
import "./Background.css"; // Ajoute le CSS séparé

const Background = () => {
  return (
    <div className="background-container" style={{ backgroundImage: "url(/assets/background_portfolio.png)" }}>
    </div>
  );
};

export default Background;
